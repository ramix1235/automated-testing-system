import React from 'react';

import logo from '../../images/logo.svg';

export default () => {
    return (
        <div>
            <img src={logo} className="logo" alt="logo" />
        </div>
    );
}